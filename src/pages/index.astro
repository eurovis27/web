---
import Layout from "../layouts/layout.astro";

import * as FrontMatter from "../components/frontmatter.md";

interface Props {
  fontColor: string;
}

const { fontColor } = Astro.props;
---

<!-- CSS -------------------------------------------------------- -->
<style define:vars={{ fontColor }}>



</style>

<!-- HTML ------------------------------------------------------- -->
<Layout title={FrontMatter.frontmatter.title} description={FrontMatter.frontmatter.description}>

    <FrontMatter.Content />

</Layout>