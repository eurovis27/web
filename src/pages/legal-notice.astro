---
import Layout from "../layouts/layout.astro";

import * as LegalNotice from '../components/legal-notice.md';

interface Props {
  fontColor: string;
}
const { fontColor } = Astro.props;
---

<!-- CSS -------------------------------------------------------- -->
<style define:vars={{ fontColor }}>



</style>

<!-- HTML ------------------------------------------------------- -->
<Layout title={LegalNotice.frontmatter.title} description={LegalNotice.frontmatter.description}>

  <LegalNotice.Content />

</Layout>